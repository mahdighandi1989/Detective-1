# ============================================================
# Detective-1 — Backend Python Dependencies
# OSINT Intelligence Encyclopedia & Person-Tracking Platform
# Python 3.11+
#
# SOURCE OF TRUTH: pyproject.toml
# This file is kept in sync with pyproject.toml via:
#   poetry export -f requirements.txt --output requirements.txt --without-hashes --with dev
#
# IMPORTANT: pyproject.toml MUST declare every package listed below
# and use the SAME pinned versions. If you edit this file, mirror the
# change in pyproject.toml and vice-versa, otherwise builds drift.
# ============================================================

# ---------- Web framework / ASGI ----------
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0
python-multipart==0.0.9

# ---------- Data validation / settings ----------
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# ---------- Database: PostgreSQL + ORM + migrations ----------
sqlalchemy==2.0.31
alembic==1.13.2
psycopg[binary]==3.2.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# ---------- Vector / semantic search ----------
pgvector==0.3.0
qdrant-client==1.10.1

# ---------- Graph database (persons relationship graph) ----------
neo4j==5.22.0

# ---------- Auth / security (JWT + RBAC + hashing) ----------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
PyJWT==2.8.0

# ---------- Background tasks: Celery + Redis ----------
celery==5.4.0
redis==5.0.7
flower==2.0.1

# ---------- LLM / search agents (OpenAI / Perplexity Sonar / Gemini) ----------
openai==1.35.7
google-generativeai==0.7.1
httpx==0.27.0
tenacity==8.4.2

# ---------- OSINT scraping / HTML parsing ----------
selectolax==0.3.21
beautifulsoup4==4.12.3
lxml==5.2.2

# ---------- Object storage (MinIO / S3 — images & files) ----------
boto3==1.34.140
minio==7.2.7

# ---------- HTTP / networking utilities ----------
requests==2.32.3
aiofiles==24.1.0

# ---------- Utilities ----------
python-dotenv==1.0.1
orjson==3.10.6
Pillow==10.4.0
python-slugify==8.0.4
tiktoken==0.7.0

# ============================================================
# Development / testing dependencies
# ============================================================

# ---------- Testing ----------
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
faker==26.0.0

# ---------- Linting / formatting / typing ----------
ruff==0.5.1
mypy==1.10.1
black==24.4.2
types-requests==2.32.0.20240712
types-python-slugify==8.0.2.20240310